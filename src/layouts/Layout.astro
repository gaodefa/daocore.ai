---
import Analytics from '@vercel/analytics/astro';

interface Props {
  title: string;
  description?: string;
  canonicalUrl?: string;
}

const {
  title,
  description = "DaoCore — The AI that actually does things. Your personal assistant on any platform.",
  canonicalUrl = new URL(Astro.url.pathname, Astro.site ?? "https://daocore.ai").href,
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="color-scheme" content="light dark" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="alternate" type="application/rss+xml" title="DaoCore Blog" href="/rss.xml" />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content="https://daocore.ai/og-image.png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content="https://daocore.ai/og-image.png" />

    <!-- Fonts: Clash Display + Satoshi from Fontshare with font-display swap for performance -->
    <link rel="preconnect" href="https://api.fontshare.com">
    <link rel="preconnect" href="https://cdn.fontshare.com" crossorigin>
    <link href="https://api.fontshare.com/v2/css?f[]=clash-display@700,600,500&f[]=satoshi@400,500,700&display=swap" rel="stylesheet">

    <title>{title}</title>

    <script is:inline>
      (() => {
        const key = 'oc-theme';
        const saved = localStorage.getItem(key);
        const theme = saved === 'light' || saved === 'dark' ? saved : 'dark';
        document.documentElement.dataset.theme = theme;
        document.documentElement.style.colorScheme = theme;
      })();
    </script>

    <style is:global>
      :root {
        /* Deep space palette with lobster accents */
        --bg-deep: #050810;
        --bg-surface: #0a0f1a;
        --bg-elevated: #111827;

        /* Lobster coral spectrum */
        --coral-bright: #ff4d4d;
        --coral-mid: #e63946;
        --coral-dark: #991b1b;

        /* Bioluminescent cyan */
        --cyan-bright: #00e5cc;
        --cyan-mid: #14b8a6;
        --cyan-glow: rgba(0, 229, 204, 0.4);

        /* Text */
        --text-primary: #f0f4ff;
        --text-secondary: #8892b0;
        --text-muted: #5a6480;

        /* Borders */
        --border-subtle: rgba(136, 146, 176, 0.15);
        --border-accent: rgba(255, 77, 77, 0.3);

        --surface-card: rgba(10, 15, 26, 0.65);
        --surface-card-strong: rgba(10, 15, 26, 0.8);
        --surface-overlay: rgba(0, 0, 0, 0.3);
        --surface-interactive: rgba(255, 255, 255, 0.1);
        --surface-interactive-hover: rgba(255, 255, 255, 0.2);
        --surface-cyan-soft: rgba(0, 229, 204, 0.15);
        --surface-coral-soft: rgba(255, 77, 77, 0.15);
        --surface-inset-highlight: rgba(255, 255, 255, 0.05);
        --newsletter-gradient:
          linear-gradient(
            135deg,
            rgba(255, 77, 77, 0.05) 0%,
            rgba(10, 15, 26, 0.8) 50%,
            rgba(0, 229, 204, 0.03) 100%
          );
        --press-featured-gradient:
          linear-gradient(135deg, rgba(255, 77, 77, 0.05) 0%, rgba(10, 15, 26, 0.7) 100%);
        --shadow-coral-soft: rgba(255, 77, 77, 0.15);
        --shadow-coral-mid: rgba(255, 77, 77, 0.25);
        --shadow-coral-strong: rgba(255, 77, 77, 0.35);
        --shadow-cyan-soft: rgba(0, 229, 204, 0.15);
        --shadow-github-soft: rgba(240, 244, 255, 0.1);
        --github-hover-color: #f0f4ff;
        --hero-title-start: #f0f4ff;
        --hero-title-end: #00e5cc;
        --logo-gradient-start: #ff4d4d;
        --logo-gradient-end: #991b1b;
        --logo-glow: rgba(255, 77, 77, 0.4);
        --logo-glow-hover: rgba(0, 229, 204, 0.6);

        /* Fonts */
        --font-display: 'Clash Display', system-ui, sans-serif;
        --font-body: 'Satoshi', system-ui, sans-serif;
        --font-mono: 'SF Mono', 'Fira Code', 'JetBrains Mono', monospace;
      }

      html[data-theme='light'] {
        --bg-deep: #fcfeff;
        --bg-surface: #ffffff;
        --bg-elevated: #f5f9ff;

        --coral-bright: #ef4b58;
        --coral-mid: #de3f4d;
        --coral-dark: #c43645;

        --cyan-bright: #008f87;
        --cyan-mid: #00766e;
        --cyan-glow: rgba(0, 143, 135, 0.24);

        --text-primary: #0b1220;
        --text-secondary: #2e405c;
        --text-muted: #5f7290;

        --border-subtle: rgba(15, 23, 42, 0.16);
        --border-accent: rgba(239, 75, 88, 0.34);

        --surface-card: rgba(255, 255, 255, 0.88);
        --surface-card-strong: rgba(255, 255, 255, 0.95);
        --surface-overlay: rgba(160, 174, 194, 0.26);
        --surface-interactive: rgba(15, 23, 42, 0.1);
        --surface-interactive-hover: rgba(15, 23, 42, 0.16);
        --surface-cyan-soft: rgba(0, 143, 135, 0.16);
        --surface-coral-soft: rgba(239, 75, 88, 0.16);
        --surface-inset-highlight: rgba(15, 23, 42, 0.06);
        --newsletter-gradient:
          linear-gradient(
            135deg,
            rgba(239, 75, 88, 0.11) 0%,
            rgba(255, 255, 255, 0.98) 52%,
            rgba(0, 143, 135, 0.12) 100%
          );
        --press-featured-gradient:
          linear-gradient(135deg, rgba(239, 75, 88, 0.13) 0%, rgba(255, 255, 255, 0.96) 100%);
        --shadow-coral-soft: rgba(239, 75, 88, 0.19);
        --shadow-coral-mid: rgba(239, 75, 88, 0.27);
        --shadow-coral-strong: rgba(239, 75, 88, 0.33);
        --shadow-cyan-soft: rgba(0, 143, 135, 0.22);
        --shadow-github-soft: rgba(11, 18, 32, 0.2);
        --github-hover-color: #0b1220;
        --hero-title-start: #c93342;
        --hero-title-end: #f04d5a;
        --logo-gradient-start: #ff7079;
        --logo-gradient-end: #ea4c59;
        --logo-glow: rgba(239, 75, 88, 0.2);
        --logo-glow-hover: rgba(0, 143, 135, 0.35);
      }

      *, *::before, *::after {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        font-family: var(--font-body);
        background: var(--bg-deep);
        color: var(--text-primary);
        line-height: 1.6;
        min-height: 100vh;
        transition: background-color 0.25s ease, color 0.25s ease;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
      }

      html[data-theme='light'] body {
        background:
          radial-gradient(1200px circle at 12% -10%, rgba(224, 53, 67, 0.04), transparent 58%),
          radial-gradient(900px circle at 88% -12%, rgba(0, 143, 135, 0.05), transparent 56%),
          var(--bg-deep);
      }

      ::selection {
        background: var(--coral-bright);
        color: var(--bg-deep);
      }

      html[data-theme='light'] .stars {
        opacity: 0.28;
        filter: saturate(0.6);
      }

      html[data-theme='light'] .nebula {
        opacity: 0.45;
      }

      .theme-toggle {
        position: fixed;
        top: 11px;
        right: 24px;
        z-index: 999;
        width: 40px;
        height: 40px;
        display: grid;
        place-items: center;
        padding: 0;
        border-radius: 999px;
        border: 1px solid var(--border-subtle);
        background: var(--surface-card-strong);
        cursor: pointer;
        backdrop-filter: blur(10px);
        transition: all 0.2s ease;
      }

      .theme-toggle:hover {
        border-color: var(--border-accent);
        transform: translateY(-1px);
        box-shadow: 0 10px 24px color-mix(in srgb, var(--coral-bright) 20%, transparent);
      }

      .theme-toggle:focus-visible {
        outline: 2px solid var(--cyan-bright);
        outline-offset: 2px;
      }

      .theme-toggle-icon {
        font-size: 1.05rem;
        line-height: 1;
        color: var(--text-secondary);
        transition: transform 0.2s ease, color 0.2s ease;
      }

      .theme-toggle:hover .theme-toggle-icon {
        color: var(--text-primary);
        transform: rotate(10deg) scale(1.04);
      }

      @media (max-width: 640px) {
        .theme-toggle {
          top: 11px;
          right: 12px;
          width: 36px;
          height: 36px;
        }
      }
    </style>
  </head>
  <body>
    <!-- Preloader: Nine Hand Seals animation, first home-page visit only -->
    <script is:inline>
(()=>{
var pn=window.location.pathname;if(pn!=='/'&&pn!=='')return
var ov=document.createElement('div');ov.id='dc-ov';ov.style.cssText='position:fixed;inset:0;z-index:999999;background:#050810;display:flex;flex-direction:column;align-items:center;justify-content:center;pointer-events:none'
var cv=document.createElement('canvas');cv.id='dc-cv';cv.style.cssText='position:fixed;inset:0;width:100%;height:100%;z-index:0'
ov.appendChild(cv)
var ch=document.createElement('div');ch.id='dc-ch';ch.style.cssText='position:relative;z-index:1;font-size:clamp(96px,24vw,200px);font-weight:700;line-height:1;opacity:0;transform:scale(0.3);font-family:\"Clash Display\",system-ui,sans-serif;color:#f0f4ff;text-shadow:0 0 40px rgba(255,77,77,0.6),0 0 80px rgba(255,77,77,0.5),0 0 160px rgba(255,77,77,0.4);transition:transform .35s cubic-bezier(.34,1.56,.64,1),opacity .2s'
ov.appendChild(ch)
var sb=null

var pg=null
var dA=[]

var br=null
Clash Display\',system-ui,sans-serif\">DAOCORE</div><div style=\"margin-top:6px;font-size:clamp(10px,1.2vw,13px);letter-spacing:.2em;color:rgba(240,244,255,.25);font-weight:400\">The AI that actually does things</div>'

var sk=document.createElement('button');sk.textContent='\u8df3\u8fc7 \u203a';sk.style.cssText='position:fixed;bottom:clamp(16px,3vh,28px);right:clamp(16px,3vw,28px);z-index:10;padding:8px 18px;border:1px solid rgba(255,255,255,.12);border-radius:6px;background:rgba(255,255,255,.04);color:rgba(255,255,255,.35);font-size:13px;font-family:inherit;cursor:pointer;opacity:0;transition:opacity .4s;pointer-events:auto'
ov.appendChild(sk)
document.documentElement.appendChild(ov)
var SE=[{c:'\u4e34',j:'Rin',m:'tenacity'},{c:'\u5175',j:'Py\u014d',m:'strength'},{c:'\u6597',j:'T\u014d',m:'wisdom'},{c:'\u8005',j:'Sha',m:'healing'},{c:'\u7686',j:'Kai',m:'control'},{c:'\u9635',j:'Jin',m:'perception'},{c:'\u5217',j:'Retsu',m:'reality'},{c:'\u5728',j:'Zai',m:'presence'},{c:'\u524d',j:'Zen',m:'completion'}]
var CO=['#ff4d4d','#ff5c5c','#ff6b6b','#e63e3e','#ff4d4d','#d63333','#ff5252','#ff7a7a','#ff4d4d']
var SS=window.speechSynthesis,zV=null
function lV(){try{zV=SS.getVoices().find(function(v){return v.lang.startsWith('zh')})||null}catch(_){}}
if(SS){SS.addEventListener('voiceschanged',lV);lV()}
function sp(t){if(!SS)return;try{window.speechSynthesis.cancel()}catch(_){}var u=new SpeechSynthesisUtterance(t);u.lang='zh-CN';u.rate=.85;if(zV)u.voice=zV;try{SS.speak(u)}catch(_){}}
var LX=cv.getContext('2d'),W,H,CX,CY
function rs(){W=cv.width=window.innerWidth;H=cv.height=window.innerHeight;CX=W/2;CY=H/2}
window.addEventListener('resize',rs);rs()
var N=140,NF=40,amb=[],foc=[]
function ra(a,b){return Math.random()*(b-a)+a}
function h2r(h){var v=parseInt(h.slice(1),16);return{r:(v>>16)&255,g:(v>>8)&255,b:v&255}}
function Pt(o){o=o||{};this.x=o.x||ra(0,W);this.y=o.y||ra(0,H);this.vx=o.vx||ra(-.3,.3);this.vy=o.vy||ra(-.3,.3);this.sz=o.size||ra(1.5,4);this.c=o.color||{r:255,g:77,b:77};this.op=o.opacity||ra(.2,.7);this.an=ra(0,Math.PI*2);this.oR=ra(70,180);this.oS=ra(.003,.01)}
Pt.prototype.tk=function(ph,tx,ty,no){switch(ph){case'idle':this.x+=this.vx;this.y+=this.vy;if(this.x<-40)this.x=W+40;if(this.x>W+40)this.x=-40;if(this.y<-40)this.y=H+40;if(this.y>H+40)this.y=-40;this.op*=.25;break
case'converge':var dx=tx-this.x,dy=ty-this.y,d=Math.sqrt(dx*dx+dy*dy),sp=Math.min(d*.06,14);if(d>2){this.x+=dx/d*sp;this.y+=dy/d*sp}this.op=Math.min(this.op,1-d/(W*.5));break
case'burst':this.x+=ra(-8,8);this.y+=ra(-8,8);this.op=Math.max(0,this.op-.05);this.sz*=ra(1.5,3);break
case'orbit':this.an+=this.oS;var ttx=tx+Math.cos(this.an)*this.oR,tty=ty+Math.sin(this.an)*this.oR*.5;this.x+=(ttx-this.x)*.07;this.y+=(tty-this.y)*.07;this.op=.2+Math.sin(no*.003+this.an)*.15;break
case'fade':this.x+=this.vx*.3;this.y+=this.vy*.3;this.op=Math.max(0,this.op-.015)}}
Pt.prototype.dr=function(lx){if(this.op<=.01)return;lx.globalAlpha=this.op;var r=this.c.r,g=this.c.g,b=this.c.b,gd=lx.createRadialGradient(this.x,this.y,0,this.x,this.y,this.sz*3);gd.addColorStop(0,'rgba('+r+','+g+','+b+',1)');gd.addColorStop(.3,'rgba('+r+','+g+','+b+',.5)');gd.addColorStop(1,'rgba('+r+','+g+','+b+',0)');lx.fillStyle=gd;lx.beginPath();lx.arc(this.x,this.y,this.sz*3,0,Math.PI*2);lx.fill()}
function iP(cl){amb=[];foc=[];var c=cl||{r:245+ra(-20,10),g:67+ra(-20,20),b:67+ra(-20,10)},gc={r:255,g:180+ra(-30,30),b:180+ra(-30,30)};for(var iN=0;iN<N;iN++)amb.push(new Pt({x:ra(-100,W+100),y:ra(-100,H+100),vx:ra(-.4,.4),vy:ra(-.4,.4),size:ra(1,4),color:c,opacity:ra(.15,.6)}));for(var iN=0;iN<NF;iN++)foc.push(new Pt({x:ra(CX-120,CX+120),y:ra(CY-120,CY+120),size:ra(.5,2),color:gc,opacity:ra(.2,.5)}))}
function dB(no){var g=LX.createRadialGradient(CX,CY,0,CX,CY,Math.max(W,H)*.55);g.addColorStop(0,'rgba(5,8,16,1)');g.addColorStop(.5,'rgba(8,12,22,1)');g.addColorStop(1,'rgba(5,8,16,1)');LX.fillStyle=g;LX.fillRect(0,0,W,H);var pu=.08+Math.sin(no*.001)*.03,ag=LX.createRadialGradient(CX,CY,0,CX,CY,Math.max(W,H)*.3);ag.addColorStop(0,'rgba(255,77,77,'+(pu*.2)+')');ag.addColorStop(1,'rgba(255,77,77,0)');LX.fillStyle=ag;LX.fillRect(0,0,W,H)}
var T={cv:280,bs:130,gl:280,fd:180},TT=T.cv+T.bs+T.gl+T.fd
var i=0,t0=0,skd=false,dn=false,raF=null,pr=-1
function go(i2){if(i2>=SE.length){dn=true;fn();return}
i=i2;t0=performance.now();var s=SE[i2],c=h2r(CO[i2%CO.length])
ch.textContent=s.c;ch.style.opacity='0';ch.style.transform='scale(0.3)';iP(c)

sp(s.c);if(i2===0)setTimeout(function(){sk.style.opacity='1'},200)}
function fn(){dn=true;ch.style.display='none'

setTimeout(function(){ov.style.transition='opacity .6s';ov.style.opacity='0';setTimeout(function(){if(ov.parentNode)ov.parentNode.removeChild(ov);if(raF)cancelAnimationFrame(raF)},700)},1800)}
sk.addEventListener('click',function(){if(skd||dn)return;skd=true;ch.style.transition='transform .35s cubic-bezier(.55,0,.7,.2),opacity .3s';ch.style.opacity='0';ch.style.transform='scale(0.85)';setTimeout(fn,250)})
document.addEventListener('keydown',function(e){if(e.key==='Escape'||e.key===' '&&!dn){skd=true;ch.style.transition='transform .35s cubic-bezier(.55,0,.7,.2),opacity .3s';ch.style.opacity='0';ch.style.transform='scale(0.85)';setTimeout(fn,250)}})
function lp(no){if(dn){dB(no);raF=requestAnimationFrame(lp);return}
if(i!==pr){go(i);pr=i}
var dt=performance.now()-t0,ph;if(dt<T.cv)ph='converge';else if(dt<T.cv+T.bs)ph='burst';else if(dt<T.cv+T.bs+T.gl)ph='orbit';else ph='fade'
if(ph==='burst'){var bp=(dt-T.cv)/T.bs;if(bp<.15){ch.style.opacity='1';ch.style.transform='scale(1)';}else if(bp<.6)ch.style.transform='scale(1.15)'}else if(ph==='orbit'){;var gp=(dt-T.cv-T.bs)/T.gl;ch.style.transform='scale('+(1+Math.sin(gp*Math.PI*4)*.04)+')'}else if(ph==='fade'){ch.style.opacity='0';ch.style.transform='scale(0.85)';var fp=(dt-T.cv-T.bs-T.gl)/T.fd;if(fp>=1){i++;pr=-1}}else if(ph==='converge'){var cp=Math.min(dt/T.cv,1),r=Math.max(W,H)*.35*(1-cp)+30;LX.strokeStyle='rgba(255,77,77,'+((1-cp)*.2)+')';LX.lineWidth=1;LX.beginPath();LX.arc(CX,CY,r,0,Math.PI*2);LX.stroke()}
dB(performance.now())
for(var pi=0;pi<amb.length;pi++){amb[pi].tk(ph,CX,CY,no);amb[pi].dr(LX)}
for(var pi=0;pi<foc.length;pi++){if(ph==='orbit'||ph==='burst')foc[pi].tk(ph,CX,CY,no);else foc[pi].tk('idle',CX,CY,no);foc[pi].dr(LX)}
if(ph==='burst'){var bp2=Math.min((dt-T.cv)/T.bs,1);if(bp2<.2){LX.fillStyle='rgba(255,255,255,'+((1-bp2/.2)*.1)+')';LX.fillRect(0,0,W,H)}}
raF=requestAnimationFrame(lp)}
iP();go(0);raF=requestAnimationFrame(lp)
})();
    </script>
    <button class="theme-toggle" type="button" data-theme-toggle aria-label="Switch color theme">
      <span class="theme-toggle-icon" data-theme-icon aria-hidden="true"></span>
    </button>
    <slot />
    <Analytics />

    <script is:inline>
      (() => {
        const key = 'oc-theme';
        const root = document.documentElement;
        const toggle = document.querySelector('[data-theme-toggle]');
        const icon = document.querySelector('[data-theme-icon]');
        if (!toggle || !icon) return;

        const applyTheme = (theme, persist = true) => {
          root.dataset.theme = theme;
          root.style.colorScheme = theme;
          const isDark = theme === 'dark';
          icon.textContent = isDark ? '☀' : '☾';
          toggle.setAttribute('aria-label', isDark ? 'Switch to light mode' : 'Switch to dark mode');
          toggle.setAttribute('title', isDark ? 'Switch to light mode' : 'Switch to dark mode');
          if (persist) {
            localStorage.setItem(key, theme);
          }
        };

        toggle.addEventListener('click', () => {
          const next = root.dataset.theme === 'light' ? 'dark' : 'light';
          applyTheme(next);
        });

        const current = root.dataset.theme === 'light' ? 'light' : 'dark';
        applyTheme(current, false);
      })();
    </script>
  </body>
</html>
